version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: shoestore-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: shoestore
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    container_name: shoestore-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/shoestore
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      JWT_SECRET: mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLongForHS256Algorithm
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    container_name: shoestore-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:
